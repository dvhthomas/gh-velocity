---
title: "Getting Started"
weight: 1
bookCollapseSection: true
---

# Getting Started

gh-velocity gives you engineering metrics from your GitHub data. There are three levels, each building on the last.

## 1. Summary reports — start here

Generate a config and run a report. That's it.

```bash
cd your-repo
gh velocity config preflight --write   # analyzes your repo, writes .gh-velocity.yml
gh velocity report --since 30d         # lead time, throughput, quality
```

No labels, no workflows, no project boards required. You get useful numbers immediately. See the [Quick Start]({{< relref "/getting-started/quick-start" >}}) for the full walkthrough.

## 2. Better classification with labels

Reports categorize issues as bug, feature, or other based on label matchers in your config. If your issues don't have consistent labels, the categories won't be accurate.

The [project-label-sync](https://github.com/dvhthomas/project-label-sync) GitHub Action solves this. It watches your GitHub Projects board and automatically applies labels to issues when they move between columns. Your board stays the source of truth, and your repo labels stay in sync — no manual labeling.

## 3. Automatic metrics on each issue and PR

`gh velocity issue 42` and `gh velocity pr 125` show composite metrics for a single item. With `--post`, they write those metrics directly into the issue or PR body.

To run this automatically every time an issue closes or a PR merges, add a small GitHub Actions workflow to the repo. The [posting guide]({{< relref "/guides/posting-reports" >}}) has the workflow file you can copy.

**This is per-repo.** GitHub Actions triggers only fire for events in their own repository. Every repo that wants automatic item-level metrics needs two files committed: `.gh-velocity.yml` (generated by `preflight --write`) and the workflow file. If you have five repos, you set up five repos. There is no cross-repo mechanism.

{{< children >}}
