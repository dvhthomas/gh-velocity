# gh-velocity configuration
# Generated by: gh velocity config preflight -R hashicorp/terraform
# Date: 2026-03-11
#
# More PRs than issues in the last 30 days — PR-centric workflow detected
# Found status labels: [in progress] — can be used for WIP tracking
# Discussions are disabled — enable them in repo settings for bulk --post
# Token scopes: gist, project, read:org, repo, workflow
# Config verification: YAML parses cleanly
# Config verification: 4 categories defined, all matchers valid

# Scope: which issues/PRs to analyze (GitHub search syntax)
# Narrow with labels, assignees, milestones, etc.:
#   query: "repo:owner/repo label:team-backend"
#   query: "org:myorg label:bug"  # org-wide
# Override per-run with: --scope 'label:"priority:high"'
scope:
  query: "repo:hashicorp/terraform"

# Issue/PR classification — assigns each item to a category.
# Each entry in 'match' is a matcher string. Supported formats:
#   label:<name>          — matches a GitHub label (case-insensitive)
#   type:<name>           — matches a GitHub Issue Type (exact)
#   title:/<regex>/       — matches the title with a Go regex
#   title:/<regex>/i      — same, but case-insensitive
# First category to match wins. Unmatched items are classified as "other".
quality:
  categories:
    - name: bug
      match:
        - "label:bug"
    - name: feature
      match:
        - "label:enhancement"
    - name: chore
      match:
        - "title:/^chore[\\(: ]/i"
        - "title:/^refactor[\\(: ]/i"
    - name: docs
      match:
        - "label:documentation"
  hotfix_window_hours: 72

# Match evidence (last 30 days of issues + PRs):
#   bug / label:bug — 14 matches, e.g. #38230 Terraform Actions ignore depends_on between action-trigge...
#   bug / title:/\bfix(es|ed)?\b/i — 5 matches, e.g. #38254 test: Fix E2E tests to use correct errors for assertions,...
#   bug / title:/^fix[\(: ]/i — 1 matches, e.g. #38190 fix: Add check that we're able to run the e2e.GoBuild met...
#   bug / label:crash — 0 matches (review this matcher)
#   bug / label:regression — 0 matches (review this matcher)
#   bug / title:/^bug[\(: ]/i — 0 matches (review this matcher)
#   feature / label:enhancement — 30 matches, e.g. #38189 The multi-attribute composite key feature for DynamoDB GSIs
#   feature / title:/^add[\(: ]/i — 6 matches, e.g. #38241 Add variable arguments to init help text
#   feature / title:/^feat[\(: ]/i — 1 matches, e.g. #38157 feat: Allow validate command to validate state_store blocks.
#   feature / title:/^feature[\(: ]/i — 1 matches, e.g. #15608 Feature request: Import resource into .tfstate AND create...
#   chore / title:/^chore[\(: ]/i — 1 matches, e.g. #38185 chore: Update go version from 1.25 to 1.25.7
#   chore / title:/^refactor[\(: ]/i — 1 matches, e.g. #38225 refactor: replace load static config calls
#   chore / title:/^ci[\(: ]/i — 0 matches (review this matcher)
#   chore / title:/^build[\(: ]/i — 0 matches (review this matcher)
#   docs / label:documentation — 1 matches, e.g. #38203 Fixing minor typo in link in docs/debugging.md
#   docs / title:/^docs?[\(: ]/i — 1 matches, e.g. #38195 docs: Update issue reference in change file for v1.15

# Commit message scanning
commit_ref:
  patterns: ["closes"]

# Cycle time strategy: pr
cycle_time:
  strategy: pr

# Lifecycle: how commands identify work stages
# Each stage has a query (GitHub search qualifiers) that commands append.
# With a project board, add project_status arrays for board-based filtering.
lifecycle:
  done:
    query: "is:closed"

# Posting readiness (for --post flag):
#
# Required scopes per feature:
#   --post to issue/PR comments:  repo
#   --post to discussions:        repo (+ discussions enabled)
#   project board queries:        project, read:org (if org project)
#
# GitHub Actions: set permissions in workflow YAML:
#   permissions:
#     issues: write        # for --post to issue comments
#     pull-requests: write  # for --post to PR comments
#     discussions: write    # for --post to discussions
#
#   discussions: disabled — enable in repo Settings → General → Features
#   token scopes: gist, project, read:org, repo, workflow

# Recent activity (last 30 days): 41 issues closed, 60 PRs merged
# Most-used labels on recent closed issues:
#   enhancement (27)
#   import (17)
#   new (15)
#   bug (13)
#   waiting-response (8)
#   cli (4)
#   config (4)
#   providers/protocol (4)
#   core (2)
#   confirmed (2)
#   cloud (2)
