# gh-velocity configuration
# Generated by: gh velocity config preflight -R kubernetes/kubernetes
# Date: 2026-03-11
#
# More PRs than issues in the last 30 days — PR-centric workflow detected
# Discussions are disabled — enable them in repo settings for bulk --post
# Token scopes: gist, project, read:org, repo, workflow
# Config verification: YAML parses cleanly
# Config verification: 4 categories defined, all matchers valid

# Scope: which issues/PRs to analyze (GitHub search syntax)
# Narrow with labels, assignees, milestones, etc.:
#   query: "repo:owner/repo label:team-backend"
#   query: "org:myorg label:bug"  # org-wide
# Override per-run with: --scope 'label:"priority:high"'
scope:
  query: "repo:kubernetes/kubernetes"

# Issue/PR classification — assigns each item to a category.
# Each entry in 'match' is a matcher string. Supported formats:
#   label:<name>          — matches a GitHub label (case-insensitive)
#   type:<name>           — matches a GitHub Issue Type (exact)
#   title:/<regex>/       — matches the title with a Go regex
#   title:/<regex>/i      — same, but case-insensitive
# First category to match wins. Unmatched items are classified as "other".
quality:
  categories:
    - name: bug
      match:
        - "label:kind/bug"
    - name: feature
      match:
        - "label:kind/feature"
    - name: chore
      match:
        - "label:kind/cleanup"
    - name: docs
      match:
        - "label:kind/documentation"
  hotfix_window_hours: 72

# Match evidence (last 30 days of issues + PRs):
#   bug / label:kind/bug — 121 matches, e.g. #137549 Pod with CAP_SYS_ADMIN and /dev/fuse hostPath mount still...
#   bug / title:/\bfix(es|ed)?\b/i — 51 matches, e.g. #137593 flaking fix: ignore watch actions from the informer's ref...
#   bug / title:/^fix[\(: ]/i — 29 matches, e.g. #137475 Fix scheduler flaky test: wait for DeviceClass cache sync...
#   bug / title:/^bug[\(: ]/i — 2 matches, e.g. #127938 bug: No 'time' added when server-side-applying the same y...
#   bug / label:kind/regression — 0 matches (review this matcher)
#   feature / label:kind/feature — 144 matches, e.g. #137526 Can "kubectl" be simplified to "kc"?
#   feature / title:/^add[\(: ]/i — 39 matches, e.g. #137323 Add geofencing to prevent running this software in Califo...
#   feature / title:/^feature[\(: ]/i — 2 matches, e.g. #136922 Feature request: Readiness Gates Controller
#   feature / title:/^feat[\(: ]/i — 1 matches, e.g. #134522 feat req: resource management for pods similar to contain...
#   chore / label:kind/cleanup — 159 matches, e.g. #137441 Remove deprecated PodRequestsAndLimits from kubectl/pkg/u...
#   chore / title:/^chore[\(: ]/i — 2 matches, e.g. #137076 chore(test/autoscaling): update HPA e2e DynamicResourceCo...
#   chore / title:/^refactor[\(: ]/i — 2 matches, e.g. #137304 refactor: fieldsv1 encapsulation via accessors and deprec...
#   chore / title:/^ci[\(: ]/i — 0 matches (review this matcher)
#   chore / title:/^build[\(: ]/i — 0 matches (review this matcher)
#   docs / label:kind/documentation — 5 matches, e.g. #126232 system:kubelet-api-admin does not have permissions to iss...
#   docs / title:/^docs?[\(: ]/i — 1 matches, e.g. #137283 docs(api): clarify env var key constraints for env-from f...
#   docs / label:sig/docs — 0 matches (review this matcher)

# Commit message scanning
commit_ref:
  patterns: ["closes"]

# Cycle time strategy: pr
cycle_time:
  strategy: pr

# Lifecycle: how commands identify work stages
# Each stage has a query (GitHub search qualifiers) that commands append.
# With a project board, add project_status arrays for board-based filtering.
lifecycle:
  done:
    query: "is:closed"
  backlog:
    query: "is:open label:priority/backlog"

# Posting readiness (for --post flag):
#
# Required scopes per feature:
#   --post to issue/PR comments:  repo
#   --post to discussions:        repo (+ discussions enabled)
#   project board queries:        project, read:org (if org project)
#
# GitHub Actions: set permissions in workflow YAML:
#   permissions:
#     issues: write        # for --post to issue comments
#     pull-requests: write  # for --post to PR comments
#     discussions: write    # for --post to discussions
#
#   discussions: disabled — enable in repo Settings → General → Features
#   token scopes: gist, project, read:org, repo, workflow

# Recent activity (last 30 days): 182 issues closed, 344 PRs merged
# Most-used labels on recent closed issues:
#   needs-triage (119)
#   kind/bug (62)
#   lifecycle/rotten (59)
#   sig/api-machinery (54)
#   triage/accepted (54)
#   kind/feature (50)
#   sig/node (42)
#   kind/flake (23)
#   needs-sig (19)
#   kind/failing-test (15)
#   sig/network (13)
#   sig/scheduling (12)
#   priority/backlog (12)
#   sig/apps (11)
#   wg/device-management (11)
#   sig/cli (11)
#   sig/storage (10)
#   priority/important-soon (9)
#   sig/scalability (7)
#   priority/important-longterm (7)
